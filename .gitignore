/.idea
.DS_Store
*.elf
*.bin
*.bzimg
*.dbg
**/target/
*.img
/kernel-rv
/kernel-la
/.crates*
/mnt
*.swp
*.swo

# Code statistics cache
/.cache/
/code-stats/

# Cargo lock — the workspace root lockfile (./Cargo.lock) is tracked for
# reproducible builds; keep ignoring nested lockfiles of excluded std
# tooling crates (each is an independent cargo project).
/xtask/**/Cargo.lock
/tee_apps/**/Cargo.lock
/uapps/**/Cargo.lock

# Python
__pycache__
venv

# Kconfig build artifacts
defconfig
.config
.config.old
.config.prepared
auto.conf
autoconf.h
target/kbuild/
/shared

.cargo/*
!.cargo/config.toml
.vscode
