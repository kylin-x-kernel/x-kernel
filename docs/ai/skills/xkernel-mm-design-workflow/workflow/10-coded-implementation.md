# Phase 10: Coded Implementation

Owner:

- Implementation Coder

Create:

- `10-implementation-report.md`

Rules:

- Implement only `09-implementation-scope.md`.
- Keep code changes inside the allowed write set.
- Update required crate-local docs when contracts, invariants, public APIs, or
  unsafe boundaries change.
- Record every changed file and design contract.
- Stop if implementation exposes a design conflict not resolved by
  `06-frozen-design.md`.
