# Phase 09: Implementation Scope Freeze

Purpose: select one bounded implementation slice from `07-task-split.md`.

Create:

- `09-implementation-scope.md`

Required content:

- frozen design run path;
- selected task ids;
- owner crate(s);
- allowed write set;
- forbidden write set;
- design contracts to implement;
- required tests and docs;
- stop conditions.

Rules:

- Do not select unrelated task-split items.
- Do not enter coding if selected tasks depend on unfinished earlier tasks.
- If the scope requires a design change, return to the design track.
