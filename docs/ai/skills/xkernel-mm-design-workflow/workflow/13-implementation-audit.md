# Phase 13: Implementation Audit

Purpose: decide whether the implementation run is complete.

Create:

- `13-implementation-audit.md`

Required checks:

- selected scope maps to `07-task-split.md`;
- implementation stayed within allowed write set;
- code review completed;
- validation completed or blockers are explicit;
- docs were updated where required;
- unresolved findings are listed with owner and follow-up phase.

Verdict values:

- `ready`;
- `needs-fix`;
- `blocked`;
- `requires-design-reopen`.
