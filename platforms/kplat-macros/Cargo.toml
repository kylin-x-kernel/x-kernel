[package]
name = "kplat-macros"
version = "0.1.0"
description = "Procedural macros for the `kplat` crate"
edition.workspace = true
authors.workspace = true
license.workspace = true
homepage.workspace = true
repository.workspace = true

[dependencies]
proc-macro2 = "1.0"
quote = "1.0"
syn = { version = "2.0", features = ["full"] }

[dev-dependencies]
crate_interface = "0.1"

[lib]
proc-macro = true
